import Vue from 'vue'
import Vuex from 'vuex'
import UrlServices from '../config/UrlServices'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    UrlServices
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
